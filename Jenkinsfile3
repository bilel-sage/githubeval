pipeline {
    agent any
    stages {
        stage('Build Image Docker') {
            steps {
                sh '''
                    docker build -t my-nginx .
                '''
            }
        }
    }
}
